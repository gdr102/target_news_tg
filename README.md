# Target News
Бот для автоматического отслеживания Telegram-каналов и пересылки постов, содержащих ключевые слова, в целевой канал/чат.

## ⚠️ Важное замечание
Для работы этого бота используется **отдельный** Telegram-аккаунт, который функционирует как пользователь-бот. Это означает:
- 📱 Нужен отдельный номер телефона для регистрации аккаунта
- 🔐 Аккаунт бота является полноценным Telegram-пользователем (не ботом в смысле @BotFather)
- 👤 Бот имеет свой профиль, имя, username (опционально)
- 🔑 Требуются API ключи как для обычного пользователя (с my.telegram.org)

- 📊 Все ограничения обычных аккаунтов применимы и к боту

## 📋 Функциональность
**Основные возможности**
- Автоматический мониторинг сообщений из добавленных каналов-источников
- Фильтрация по ключевым словам с регистронезависимым поиском
- Автоматическая пересылка совпадающих сообщений в целевой канал
- Обработка защищенных каналов - отправка копии текста при невозможности пересылки

**Управление источниками**
- /sources - показать список всех отслеживаемых каналов
- /add_source @username - добавить новый канал для отслеживания
- /remove_source @username - удалить канал из отслеживания

**Управление ключевыми словами**
- /keywords - показать список всех ключевых слов
- /add_keyword "слово" - добавить новое ключевое слово
- /remove_keyword "слово" - удалить ключевое слово

**Вспомогательные команды**
- /help - показать справочную информацию по всем командам

## 📁 Структура проекта
```
project/
├── app/
│   ├── handlers/
│   │   ├── get/
│   │   │   ├── all_channels.py      # обработчик /sources
│   │   │   ├── all_keyword.py       # обработчик /keywords
│   │   │   └── new_message.py       # обработчик входящих сообщений
│   │   ├── changes/
│   │   │   ├── add_keyword.py       # обработчик /add_keyword
│   │   │   ├── add_source.py        # обработчик /add_source
│   │   │   ├── remove_keyword.py    # обработчик /remove_keyword
│   │   │   └── remove_source.py     # обработчик /remove_source
│   │   └── other/
│   │       └── help.py              # обработчик /help
│   ├── functions/
│   │   ├── other.py                 # вспомогательные функции
│   │   ├── read_json.py             # чтение JSON файлов
│   │   └── write_json.py            # запись JSON файлов
│   └── storage/
│       ├── config.py                # конфигурация
│       └── pattern.json             # ключевые слова
├── core.py                          # основной обработчик
├── main.py                          # точка входа
└── README.md                        # документация
```

## ⚙️ Использование
1. Запуск бота:
```
python main.py
```
2. Добавление каналов для отслеживания:
- Отправьте в целевой канал команду /add_source @username_канала
- Бот автоматически присоединится к каналу
3. Настройка ключевых слов:
- Добавьте ключевые слова командой /add_keyword "ключевое слово"
- Просмотрите текущий список командой /keywords
4. Проверка работы:
- При появлении сообщения с ключевым словом в отслеживаемых каналах, бот автоматически перешлет его в целевой канал


## 🔧 Особенности реализации
**Обработка сообщений**
- Проверяет только сообщения из каналов (не приватные чаты)
- Регистронезависимый поиск ключевых слов
- Автоматическое определение типа пересылки (прямая пересылка или копия)

**Безопасность**
- Удаление командных сообщений после выполнения
- Форматирование ссылок на пользователей
- Обработка ошибок с отправкой уведомлений

**Хранение данных**
- Ключевые слова хранятся в JSON файле
- Список каналов управляется через Telegram API
- Конфигурация в отдельных файлах

**⚠️ Ограничения**
- Бот должен быть администратором в целевом канале
- Для добавления каналов бот должен иметь возможность присоединяться к ним
- Защищенные каналы могут ограничивать пересылку сообщений
- API Telegram имеет ограничения на количество запросов

## 📝 Примечания
- Все команды работают только в целевом канале
- Сообщения команд автоматически удаляются после выполнения
- Бот логирует свою активность в консоль
